<h1>Корзина</h1>

{{#if courses.length}}
	<table class="responsive-table">
		<thead>
			<tr>
				<th>Название</th>
				<th>Количество</th>
				<th>Действия</th>
			</tr>
		</thead>
		<tbody>
			{{#each courses}}
				<tr>
					<td>{{title}}</td>
					<td>{{count}}</td>
					<td>
						<button type="button" class="btn btn-small grey darken-4">
							  <i class="material-icons">close</i>
						</button>
					</td>
				</tr>
			{{/each}}
		</tbody>
	</table>
	<p class="right">
		<strong>Цена: </strong>
		<span class="course-price">{{price}}</span>
	</p>
{{else}}
	<p>Корзина пуста</p>
{{/if}}